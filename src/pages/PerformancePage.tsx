import { Container, Typography } from "@mui/material";

const PerformancePage: React.FC<{}> = () => {
  return (
    <Container maxWidth={false} disableGutters>
      <Typography>Performance Page</Typography>
    </Container>
  );
};

export default PerformancePage;
